load("@rules_cc//cc:defs.bzl", "cc_library")

package(default_visibility = ["//visibility:public"])

cc_library(
    name = "openai_cpp",
    hdrs = glob(["include/openai/**/*.hpp"]),
    includes = ["include/openai"],
    deps = ["@curl//:curl"],
)
